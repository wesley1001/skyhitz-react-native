var StackTraceParser = require('./index');

function* idMaker(){
  var index = 0;
  while(index < 3)
    yield index++;
  foo();
}
var gen = idMaker();

exports.next = gen.next;

exports.lines = function() {
 try { gen.next() } catch(ex) {var e=ex;var lines = StackTraceParser.parse(ex.stack);}
console.log(e);
return lines;
}
